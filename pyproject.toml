[project]
name = "coursecraftai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pre-commit>=4.6.0",
    "pre-commit-hooks>=6.0.0",
    "semgrep>=1.168.0",
]
